{% extends 'admin/base_admin.html.twig' %}
{% block css %}
    <style>
        .field {
            padding: 3px 12px;
            border: 2px #616569 !important;
        }

        legend {
            width: auto;
        }

        .ctn-btn {
            position: absolute;
            top: 57%;
            transform: translateY(-50%);
        }

        .btnapath {
            font-size: 13px;
        !important;
        }


    </style>
    <link rel="stylesheet"
          href="{{ asset('assets/admin/plugins/datatables/datatables.css') }}">

    <!-- DataTables -->

{% endblock %}

{% block body %}
    <div
            class="card card-primary card-success">
        <div class="card-header">
            <h3 class="card-title ">
                Liste des clients
            </h3>
        </div>
        <!-- /.card-header -->
        <div class="card-body table-responsive">
            {% block filtre %}

            {% endblock %}
            <table id="example1" class="table table-bordered table-striped text-nowrap table-valign-middle">
                <thead>
                <tr>
                    <th>Nom</th>
{#                    <th>Prénom</th>#}
                    <th>Telephone</th>
                    <th></th>
                </tr>
                </thead>
                <tbody>
                {% for client in clients %}
                    <tr>
                        <td>{{ client.nomclient }}</td>
{#                        <td>{{ client.prenom }}</td>#}
                        <td>{{ client.telephone }}</td>
                        <td></td>
                    </tr>
                {% endfor %}
                </tbody>
            </table>


        </div>
        <!-- /.card-body -->
    </div>
{% endblock %}
{% block javascript %}
<!-- DataTables  & Plugins -->
<script src="{{ asset('assets/admin/plugins/datatables/jquery.dataTables.js') }}"></script>
<script src="{{ asset('assets/admin/plugins/datatables/datatables.js') }}"></script>

	<script>

    $(function () {
        $("#example1").DataTable({
            "responsive": true, "lengthChange": false, "autoWidth": false,
            "buttons": ["copy", "csv", "excel", "pdf", "print", "colvis"],
             language: {
        url: '//cdn.datatables.net/plug-ins/2.0.8/i18n/fr-FR.json',
    },
        }).buttons().container().appendTo('#example1_wrapper .col-md-6:eq(0)');
        $('#example2').DataTable({
            "paging": true,
            "lengthChange": false,
            "searching": false,
            "ordering": true,
            "info": true,
            "autoWidth": false,
            "responsive": true,
        });
    });

	</script>

{% endblock %}

