<form method="post" action="{{ path('app_services_delete', {'codeService': service.codeService}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ service.codeService) }}">
    <button class="btn">Delete</button>
</form>
