{% extends 'admin/base_admin.html.twig' %}

{% block body %}
    <div class="card card-success">
        <div class="card-header">
            <h5>Ajout d'un slider</h5>
        </div>
        {{ form_start(form) }}


        <div class="card-body">
            {{ form_widget(form) }}

        </div>
        <div class="card-footer">
            <div class="float-end">
                <a class="btn btn-primary" type="button" href="{{ path('admin_services_index') }}">
                    <i class="fa fa-arrow-left"></i>
                    Retour à la liste des services</a>

                <button class="btn btn-success" type="submit"><i class="fa fa-check"></i> Enregistrer</button>
                <button class="btn btn-warning" type="reset" value="Annuler"><i class="fa fa-times"></i> Annuler
                </button>
            </div>

        </div>

        {{ form_end(form) }}

    </div>




{% endblock %}
